use pyo3::prelude::*;
use pyo3::types::prelude::*;

/// bioworkflow Python 模块n///n/// 提供Workflow务，图，调度等务的务///n#[pymodule]
fn bioworkflow(_py:ython, _md: &ound) -> PyResult<()> {
    Ok(())n}

#[pymain]
fn main {
    // 初始化日志等n    // Python绑定n}
EOF'